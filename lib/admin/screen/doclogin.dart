import 'package:flutter/material.dart';

class DocLogin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Login mr Doctor"),
    );
  }
}
